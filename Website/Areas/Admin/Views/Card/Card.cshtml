@model IEnumerable<Website.Models.Card>

@{
    ViewData["Title"] = "Quản lý Thẻ thư viện";
    Layout = "~/Views/Shared/Master.cshtml";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="Create">Tạo thẻ mới</a>
</p>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Tìm kiếm:
            <input type="text" name="search" value="@ViewData["CurrentSearch"]" />
            <input type="submit" value="Tìm" class="btn btn-primary" /> |
            <a asp-action="Index">Xem tất cả</a>
        </p>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>Mã thẻ</th>
            <th>Tên chủ thẻ</th>
            <th>Email</th>
            <th>Điện thoại</th>
            <th>Ngày tạo</th>
            <th>Trạng thái</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.idCard</td>
                <td>@item.nameCard</td>
                <td>@item.emailCard</td>
                <td>@item.phoneCard</td>
                <td>@item.dateCard.ToShortDateString()</td>
                <td>@item.statusCard</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.idCard">Sửa</a> |
                    <a asp-action="Delete" asp-route-id="@item.idCard">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>